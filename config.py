# config.py

# List Classes
Classes = [] #<-- classes goes here

# Number of images to download per class
images_per_class = 100 #max per search capped at 100

# Directory where the dataset will be saved
save_root = "../dataset"

# Image size to resize all images to (width, height)
resize_dim = (128, 128) #<-- specify required size
